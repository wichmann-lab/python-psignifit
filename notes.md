assumes psignifit installed

install flask:
`pip install Flask`

run script with:
`psignifit-flask-app.py'
will run on localhost:5000
alter `app.run("\<ipaddress"\>, "\<port"\>])` as necessary (0.0.0.0 will allow network-wide access)
